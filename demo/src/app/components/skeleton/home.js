import React, {Component} from "react";

export default class HomeSkeleton extends Component{
  render() {
    return (
      <div>
        Something fancy like facebook loading screens or something
        similar to atlassian loading screens but like the home page
      </div>
    );
  }
}